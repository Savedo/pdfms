$('.main').html(<%= render_js_string(partial: 'show_folder', locals: { folder: folder }) %>)
<% if flash.present? %>
$('.main').prepend(<%= render_flash.to_json.html_safe %>)
<% end %>
$('.selector-host').selectorHost()
